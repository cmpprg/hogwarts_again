<section class="list">
  <% @professor.students.each do |student| %>
    <p><%= student.name %></p>
  <% end %>
</section>
<section class="average-age">
  <p>Average Age: <%= @professor.average_student_age %></p>
</section>
